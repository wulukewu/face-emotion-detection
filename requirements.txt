# --- Core Data Processing ---
ipykernel
numpy<2.0.0
pandas
matplotlib
seaborn
joblib

# --- Machine Learning ---
scikit-learn

# --- Image Processing ---
# Limit OpenCV version to work with NumPy 1.x
opencv-python<4.10.0
scikit-image

# --- Web Application Framework ---
streamlit
streamlit-webrtc
av

# --- Deep Learning (TensorFlow) ---
# Platform-specific TensorFlow installation:
# 1. For Mac (Darwin) with ARM architecture (M1/M2/M3), install Apple optimized version
tensorflow-macos; sys_platform == 'darwin' and platform_machine == 'arm64'
tensorflow-metal; sys_platform == 'darwin' and platform_machine == 'arm64'

# 2. For Windows (win32) or Linux (linux), install standard version
tensorflow; sys_platform != 'darwin' or platform_machine != 'arm64'